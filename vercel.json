{
  "version": 2,
  "framework": null,
  "buildCommand": false,
  "installCommand": "npm install --production",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
